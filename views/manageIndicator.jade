extends baseLayout

block middle

    div#content
        form(action="/expert/createIndicator", method="post")
            input(type="hidden", name="user_id", value=user.user_id)
            if user.user_group_id
                input(type="hidden", name="user_group_id", value=user.user_group_id)

            div.formBlock
                h5 Indikator
                div
                    p Name*:
                        input(type="text", name="name")
                div
                    p Beschreibung:
                        textarea(id="description")
            div.formBlock
                h5 Bewertungsfaktor
                div
                    p Zielerreichungsfaktor*:
                        input(type="text", name="target_factor")
            div.formBlock
                h5 Details (optional)
                div
                    p Quellenverweis:
                        textarea(id="source")
                //if sets
                //    div
                //        p Indikatoren-Set:
                //                each set, key in sets
                //                    div= set.name
                //                        input(type="checkbox", name="sets", value=set.indicator_set_id)

            div.formBlock.submit
                button(type="submit") Speichern